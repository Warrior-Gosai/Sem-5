SELECT * FROM TAB           /*FOR SHOW HOW MANY TABLES ARE CRREATED*/

SELECT *FROM TABLE NAME     /*FOR SHOW TABLES DETAILS*/

CREATE TABLE BRANCH_MSTR(   /*FOR CREATE TABLE*/
 BRANCH_NO VARCHAR2(15),
 BRANCH_NAME VARCHAR2(15));

INSERT INTO BRANCH_MSTR VALUES('B1112','DARBARGHADHA_BRANCH');/*FOR INSERT DATA INTO TABLE*/

DELETE FROM BRANCH_MSTR BRANCH_NO='B1112';/*FOR DELETE DATA FROM TABLE*/
 
 /*APPLY CONSTRAINTS WHEN YOU CREATE TABLE */
		/* COLUMN LEVEL CONTRAINTS*/
CREATE TABLE PRODUCT(
PID NUMBER(3) CONSTRAINTS PKPID PRIMARY KEY,
PNAME VARCHAR2(30) NOT NULL,
QOH NUMBER(3) CONSTRAINTS CKQOH CHECK(QOH>0),
PRICE NUMBER(5,2));

/* ALTER COMMANDS */
	/*ADD COMMAND*/
/*CONSTRAINTS <PRIMARY KEY & FORIEN KEY>*/
 
CREATE TABLE ACMASTER(
ACNO NUMBER(3)CONSTRAINTS PKACNO PRIMARY KEY,
NAME VARCHAR2(10),
ADATE DATE,
BAL NUMBER(9,2));

CREATE TABLE ACTRAN(
TNO NUMBER(3),
TDATE DATE,
AMOUNT NUMBER(7,2),
TYPE CHAR(1),
ACNO NUMBER(3) REFERENCES ACMASTER);

INSERT INTO ACMASTER VALUES (&ACNO,'&NAME','&ADATE',&BAL);
INSERT INTO ACTRAN VALUES (&TNO,'&TDATE',&AMOUNT,'&TYPE',&ACNO);

ALTER TABLE ACTRAN ADD USERNM VARCHAR2(10);

ALTER TABLE ACTRAN ADD CONSTRAINTS TNO UNIQUE UKTNO (TNO);
 ALTER TABLE ACMASTER DROP CONSTRAINTS PKACNO ;
 ALTER TABLE ACMASTER MODIFY NAME VARCHAR2(8);
  ALTER TABLE ACMASTER MODIFY NAME NOT NULL;